apiVersion: v1
kind: Service
metadata:
  name: commsrelay-service
  labels:
    app: commsrelay
  namespace: yorch
spec:
  selector:
    app: commsrelay
  type: ClusterIP
  ports:
    - targetPort: 8080
      port: 80
